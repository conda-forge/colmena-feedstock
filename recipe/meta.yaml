{% set name = "colmena" %}
{% set version = "0.4.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/exalearn/colmena/archive/refs/tags/v{{ version }}.tar.gz
  sha256: c4c0f2672cc7b66f2f69120e524e404c76b0f082df1d2f3fea0ed8b48bc3944b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - numpy
    - parsl >=1
    - proxystore >=0.3.3
    - pydantic ==1.*
    - python >=3.6
    - redis-py ==3.4.*

test:
  imports:
    - colmena
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/exalearn/colmena
  summary: 'colmena: Intelligent Steerable Pipelines on HPC'
  license: Apache-2.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - WardLT
    - jan-janssen
